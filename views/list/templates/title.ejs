<h1 class="text-center">
	<a href="/list/<%= list._id %>"><%= list.name %></a>
	<% if (locals.listProgress) { %>
		<small title="List sorted percentage">- <%= (listProgress * 100).toFixed(0) %>%</small>
	<% } %>
</h1>