<%- include('templates/header', { user: user }); -%>
<h1><%= list.name %> (<%= (listProgress * 100).toFixed(2) %>%)</h1>
<div class="d-flex justify-content-between align-items-center">
	<h2><%= comparison.a.name %></h2>
	<div class="btn-group" role="group" aria-label="Item Actions">
		<a class="btn btn-primary" href="/list/<%= list._id %>/<%= comparison.a._id %>/<%= comparison.b._id %>/-1"><</a>
		<a class="btn btn-primary" href="/list/<%= list._id %>/<%= comparison.a._id %>/<%= comparison.b._id %>/0">===</a>
		<a class="btn btn-primary" href="/list/<%= list._id %>/<%= comparison.a._id %>/<%= comparison.b._id %>/1">></a>
	</div>
	<h2><%= comparison.b.name %></h2>
</div>

<%- include('templates/footer', { user: user }); -%>